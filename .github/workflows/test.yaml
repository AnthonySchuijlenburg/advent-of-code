name: Run tests
on:
    push:
        branches: ['main']
    pull_request: {}
jobs:
    test:
        runs-on: ubuntu-latest
        steps:
            - uses: actions/checkout@v2
            - run: |
                  npm install
                  npm run test
